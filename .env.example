# Rails Environment Configuration Template
# Copy this file to .env and update with your values
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your actual values
#   rails server  (for local development)
#   OR
#   ./setup.sh && docker compose up  (for Docker development)

# === Docker Configuration ===
# Project name for Docker containers (change this to your project name)
COMPOSE_NAME=rails_multitenant

# === Database Configuration ===
# PostgreSQL connection details
# For local development: use localhost
# For Docker development: use 'db' (set automatically in docker-compose.yml)
DATABASE_HOST=localhost
DATABASE_NAME=rails_seed_multitenant_development
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres

# === Application ===
# Generate a new secret with: rails secret
SECRET_KEY_BASE=your_secret_key_here_generate_with_rails_secret

# === JWT Authentication ===
# Generate a secure key with: openssl rand -hex 64
JWT_SECRET_KEY=your_jwt_secret_key_here_generate_with_openssl

# === Google OAuth (Optional) ===
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# === Email Configuration (Optional) ===
# For password reset emails
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_DOMAIN=example.com
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=noreply@example.com

# === Application Settings ===
# Host URL for email links and redirects
APP_HOST=localhost:3000
APP_PROTOCOL=http

# Rails environment
RAILS_ENV=development
RACK_ENV=development

# === Optional: Redis (for caching/jobs) ===
REDIS_URL=redis://localhost:6379/0

# === Optional: Performance Monitoring ===
# Uncomment if using services like New Relic, Sentry, etc.
# NEW_RELIC_LICENSE_KEY=
# SENTRY_DSN=
