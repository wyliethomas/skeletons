# Docker Compose project name
COMPOSE_NAME=project-name

# This project uses Figaro for environment variable management
#
# To set up your environment:
# 1. Copy config/application.yml.example to config/application.yml
# 2. Update config/application.yml with your actual values
# 3. Never commit config/application.yml (it's gitignored)
#
# See config/application.yml.example for all available configuration options
